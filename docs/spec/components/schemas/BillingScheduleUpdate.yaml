type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ billing_schedule_update ]
#      attributes:
##        $ref: './BillingScheduleAttributes.yaml'
        #TODO - maybe this is not used in future